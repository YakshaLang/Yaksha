#include "whereami.h"
WAI_FUNCSPEC int WAI_PREFIX(getExecutablePath)(char *out, int capacity,
                                               int *dirname_length) {
  *dirname_length = 0;
  return 0;
}
WAI_FUNCSPEC int WAI_PREFIX(getModulePath)(char *out, int capacity,
                                           int *dirname_length) {
  *dirname_length = 0;
  return 0;
}